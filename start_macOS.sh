#!/bin/bash
clear
#------------------------------------------------------------------
# set relative path from run.sh
#------------------------------------------------------------------
path=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
cd ${path}
#------------------------------------------------------------------
# execute .jl code(s)
#------------------------------------------------------------------
julia --optimize=3 --threads 8 --check-bounds=no --project=.